# Local secrets — never commit your real API key
config.js
.env

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (only relevant if you later add tooling e.g. a linter or bundler)
node_modules/